class User < ActiveRecord::Base
  # (Remember to create a migration?)
	
	# I don't think this class needs anything, since it's not associated with any
	# of the other data tables. It's used just to limit access to the site.
end
